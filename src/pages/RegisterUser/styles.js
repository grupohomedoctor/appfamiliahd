import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  form: {
    flex: 1,
    backgroundColor: '#fff',
  },
  headerContainer: {
    paddingRight: 50,
  },
  separator: {
    width: '100%',
    height: 20,
  },
});

export default styles;
